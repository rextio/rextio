[package]
name = "rextio_runtime"
version = "0.1.0"
edition = "2021"
description = "Reserved helper runtime for generated Rextio Rust code (not yet wired; see README)."
license = "MIT"

[lib]
name = "rextio_runtime"
path = "src/lib.rs"

[dependencies]
