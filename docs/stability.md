# Feature stability

Rextio **0.1.4** is the published package version (PyPI 2026-07-18; plugin API
**1.3**; tooling contract **2.2.0**), superseding **0.1.3** (contract
**2.1.0**). The current Release Train C branch keeps package version **0.1.4**
but advances the **unreleased** producer to plugin API **1.4** and tooling
contract **2.24.0** (host source/artifact/executable planning from the
intermediate **2.3.0** shape, plus plugin standalone-artifact capability
presence/resolution in **2.4.0**, external-source inventory evidence in
**2.5.0**, C6.1 authorization-contract evidence in **2.6.0**, plus C6.2
host-extension wheel artifact-evidence preview in **2.7.0**, the opt-in
required preview-evidence gate in **2.8.0**, and the macOS/Linux direct native
runtime linkage inventory preview in **2.9.0**, followed by C6.5's always-
blocked distribution-authorization readiness assessment in **2.10.0**, and the
bounded source-transformation observation in **2.11.0**, and the exact
reachable-Cargo component-license string observation in **2.12.0**, C6.8's
one-hop direct native path-resolution observation in **2.13.0**, and C6.9's
bounded static native-runtime graph observation in **2.14.0**, followed by
C6.10's scoped source-transformation replay verification in **2.15.0**, and
C6.11's scoped Cargo license-policy receipt in **2.16.0**, C6.12's scoped
project-source license-policy receipt in **2.17.0**, and C6.13's scoped
analysis-input verification receipt in **2.18.0**, C6.14's compact
artifact-policy coverage inventory in **2.19.0**, and C6.15's scoped
artifact-class policy verification in **2.20.0**, followed by strict Full-C6
authority primitives in **2.21.0**, bounded C5.2/initial CLI coordination in
**2.22.0**, and exact technical-template/bootstrap v2 plus explicit owner
completion/finalization in **2.23.0**, followed by the public support-lock
bootstrap and path-free sandbox/support receipt surface in **2.24.0**). Those
additions are not yet part of the
PyPI 0.1.4 package. This page is the source of truth for which features are
covered by the [versioning policy](versioning.md)'s stability promises and which
are experimental (correct-but-incomplete, and free to change). Train C's local
artifact authorization does not authorize merging `0.1.5` to `main`, tagging,
or publishing to PyPI.

Tiers:

- **Stable** — part of the public contract, governed by the
  [versioning policy](versioning.md): a breaking change to a Stable feature may still
  land in a `0.MINOR` release (this is `0.x`), but it is deprecation-warned where
  possible and always called out in the [changelog](../CHANGELOG.md).
- **Experimental** — usable but incomplete; behavior, flags, and output may change in
  any release with no deprecation period. Most are off by default and opt-in.
- **Planned** — recognized by config/validation but not implemented; using it is
  rejected with a clear diagnostic, not silently mis-built.

## Core

| Feature | Tier | Notes |
| --- | --- | --- |
| Direct-Rust codegen for the typed subset (PyO3) | Stable | Scalars, lists, simple control flow, indexing, native↔native calls. Anything outside the subset is rejected (`RXTxxx`) or kept on fallback. |
| CPython fallback packaging + generated wrappers | Stable | Preserves Python semantics for everything not lowered to native. |
| Scalar boundary calls (`RXT075`/`RXT076`) | Stable | A marked native may call a scalar-signature fallback function in-process; loop-positioned calls (including comprehension bodies) are rejected; crossings count toward the threshold. Scalars cross by value (argument identity `is` is not preserved; `None`/`bool` singletons are), and a callee exception's traceback includes the runtime hook's frames - exception type and message stay CPython-exact. |
| `@rextio.native` / `@rextio.exempt` decorators | Stable | Public API. See [versioning](versioning.md). |
| `rextio.toml` configuration schema | Stable | Keys are part of the contract. |
| `[toolchain]` selection + version pins | Experimental | New in 0.1.0: tool paths, rustup channel, CPython targeting, strict verification pins. Semantics may be refined before the first non-alpha release. |
| Diagnostics (`RXTxxx` codes + messages) | Stable | Deterministic and tested; treated as a contract. |
| Native build orchestration (maturin / Cargo) | Stable | `cargo` is the default `[rust] build_tool` (the builder needs no extra Python dependency; Cargo itself must be on PATH or configured via `[toolchain]`). Set `--rust-build-tool=maturin` (or `[rust] build_tool = "maturin"`, requires the optional `rextio[build]` dependency) to build wheels with maturin; if maturin is selected but not installed Rextio automatically falls back to Cargo. If the native build fails, packaging still produces a fallback-only pure-Python (`py3-none-any`) wheel that works through the Python fallback. |
| Import policy — `fallback` | Stable | Treats an external package as fallback-only at the boundary. |
| Import policy — `analyze` / `try-native` | Experimental | Normally planning metadata. C5.1 permits one exact distribution/version/depth-1 declaration to produce non-building source inventory. The separate strict 2.22.0-2.24.0 Alpha links only direct typed scalar leaf calls for one SourceLock-authorized exact package; general external-source lowering remains unimplemented. |
| Import policy — `plugin` | Experimental | A package covered by an installed plugin is routed through it. API 1.1 concretely lowers through claim/lower hooks; API 1.2 adds keyword/literal metadata and fusion trees; API 1.3 (shipped in core 0.1.3; published core 0.1.4 still advertises 1.3) adds opaque resident values/native chaining, static receiver/callable/declared-schema metadata, and version-gated bool/string keyword literals; unreleased Train C API 1.4 adds optional standalone artifact capability (see the [plugin lowering spec](specs/plugin-lowering.md)). The analyzer remains the acceptance authority. |

## CLI

| Command | Tier | Notes |
| --- | --- | --- |
| `rextio init` / `check` / `build` / `generate` / `clean` | Stable | Documented flags are part of the contract. |
| `rextio bench` | Stable | Prints a structured native-vs-fallback comparison; timings are not asserted. |
| `rextio policy bootstrap-support-lock` (Train C, unreleased) | Experimental | Creates or exactly reuses one canonical mode-`0600` Full-C6 host support lock below an existing owner-private mode-`0700` parent. Its host-absolute-path-free JSON retains the project-relative config path and returns the target, fixed roles, raw/Merkle digests, and exact lock path/SHA-256 config pair; it authorizes neither build nor distribution. Exact/ancestor/descendant output aliases with any configured lifecycle artifact or `imports.packages.*.source_archive` fail before output creation. |
| `rextio policy finalize` (Train C, unreleased) | Experimental | Offline create-or-exact-reuse finalizer for one canonical Full-C6 bootstrap v2 plus explicit owner completion. Produces canonical manifest v2; it does not build, sign, give legal advice, or authorize distribution. |
| `--format json` / `--verbose` / `--quiet` | Stable | Result on stdout, diagnostics on stderr. |

## Experimental

| Feature | Tier | How to reach it |
| --- | --- | --- |
| Scalar-helper embedding | Experimental | `--embed-helpers` / `[embedding] enabled`. Unmarked numeric scalar helpers (single arithmetic return expression) are embedded as internal native functions through the normal checked lowering, so overflow/divide-by-zero raise like any native code. Embedded helpers lower through the checked AOT path; there is no runtime JIT compilation. |
| Numba external accelerator | Experimental (external) | `numba.jit`/`njit`/`vectorize`/`guvectorize` decorators keep a function on the Python fallback cleanly and are labeled in reports. Semantics are Numba's contract (e.g. nopython int overflow wraps), not Rextio's native contract. `--fallback=nuitka` coexists automatically (accelerated modules are kept as plain Python); Nuitka executables and the `nuitka` hybrid runtime fail early with guidance. |
| Native `try`/`except`/`finally` (built-in exceptions) | Experimental | A restricted subset compiles to native Rust: `except` handlers for built-in exceptions only (`ValueError`, `KeyError`, `IndexError`, `ZeroDivisionError`, `TypeError`, `OverflowError`, `ArithmeticError`, `RuntimeError`, `Exception`), no `try ... else`, no `as` binding, no `return` and no `break`/`continue` targeting a loop outside the block, and no non-comprehension variable first-assigned inside a block. Anything outside the subset falls back (RXT080 shim when explicitly `@rextio.native`, otherwise Python fallback). Known limitation: if the `finally` body itself raises, the original pending exception is dropped without `__context__` chaining (CPython preserves it as the new exception's context) — an accepted divergence (see [unsupported features](unsupported-features.md)); direct-native functions containing a `finally` block carry an `RXT090` note. |
| Runtime-semantics shim (`RXT080`) | Experimental | Auto-applied to explicitly `@rextio.native` dynamic/async functions; emits a generic shim that calls back into Python. |
| Native top-level module initialization | Experimental | `[policy] native_top_level`. Lowers a restricted subset of module-level code. |
| Host source graph / `ModuleInitIR` planning (Train C, unreleased) | Experimental | `check`, `generate`, and `build` expose one hash-coherent `host_source_plan`. It is descriptive-only and imports no project modules. See [host source-AOT and executables](source-aot-and-executables.md). |
| Resolved `ArtifactProfile` records and explicit Rust-executable fallback (Train C, unreleased) | Experimental | Generate/build plans describe only requested `host-extension`, `host-executable`, and `rust-crate` outputs. Rust executable fallback is exactly `error`, `python-subprocess`, or `nuitka-sidecar`; fallback-only work does not resolve a host profile. |
| Rust executable initializer-before-main slice (Train C, unreleased) | Experimental | With `native_top_level`, a one-module/no-import graph may run exact plain scalar-literal assignments before argv/entrypoint. Values are discarded, not published as Rust globals; any uncertainty blocks before Cargo. |
| External pure-Python source inventory/gate (C5.1, unreleased) | Experimental | Exactly one config-pinned, imported distribution may be inspected without import/execution when it is a verified purelib `py3-none-any` wheel with contained, non-symlink, SHA-256/size-matching depth-1 UTF-8 source. Reports expose sanitized lexical candidate evidence plus a GNU/copyleft/legal warning. It is preview-only and non-distributable; every plan blocks build before configured toolchain/artifact work. There is no project-call linkage or Rust lowering. |
| External-source SourceLock authorization contract (C6.1, unreleased) | Experimental | Project-owned `rextio.external-source.lock.json` may authorize one available C5.1 plan via exact identity, path/SHA-256/size material, custom source inventory (not full SPDX/CycloneDX), provenance with subject snapshot binding, and closed license attestation. Fail-closed offline verification; null licenses never verify; Rextio never auto-approves licenses. In ordinary preview mode verified authorization still reports `external-source-c5-not-implemented`; only the separate strict 2.21.0-2.24.0 Alpha can create bounded build/distribution authority. |
| Host-extension wheel artifact-evidence preview (C6.2, unreleased) | Experimental | Successful ordinary host-extension wheel builds may emit bounded incomplete CycloneDX 1.6 and unsigned in-toto/SLSA provenance sidecars plus additive `build.json.artifact_evidence`. No signatures, reproducibility, hermeticity, completeness, or external-source authorization claims. Host-executable, rust-crate, Nuitka sidecars, WASM, and external-package source-native builds are out of scope. |
| Required artifact-evidence gate (C6.3, unreleased) | Experimental | `[build] artifact_evidence_policy = "required"` fails closed unless the exact single host-extension + CPython wheel path produces `preview-ready` C6.2 evidence. It rejects executable/crate/additional profiles before external toolchain work. Satisfaction is still incomplete, unsigned, and explicitly not distribution authorization. |
| Direct native runtime linkage inventory (C6.4, unreleased) | Experimental | For the same bounded host-extension + CPython wheel, isolated absolute-tool/no-shell inspectors read a private wheel-bound same-byte snapshot and record macOS `otool -L` or Linux `readelf -W -d` direct linkage. The original/snapshot identity and digest are revalidated. It records normalized architecture plus closed-allowlist dependencies with `origin` and stable `bom_ref`; a Mach-O first-row Cargo-private self-ID or exact stable `@rpath/lib_rextio_native.dylib` is excluded only with `MH_DYLIB`, the expected basename, and exact bounded `otool -D` agreement. Best-effort failure keeps the wheel with fixed-reason `unavailable` evidence; required mode emits `RXT060`, while pinned-parent receipt checks preserve pre-existing/concurrent content and report mismatched rollback as incomplete. No path resolution, transitive closure, `dlopen`, Windows PE, runtime-bearing plugins, or signatures; successful evidence remains incomplete, unsigned, and non-authorizing. |
| Distribution-authorization readiness assessment (C6.5-C6.15, unreleased) | Experimental | The same in-scope evidence path adds immutable `artifact_distribution_authorization` after final evidence/required transaction handling. It is always `blocked` with readiness-only authority and false completeness/signature/authorization fields. Policy version 11 has fourteen observation checks and ten readiness checks, adding C6.15's exact class-policy declaration binding. Missing C6.15 makes only `scoped-artifact-class-policy-declaration-bound` unavailable with its dedicated blocker; malformed or forged present receipts degrade to all `not-evaluated` plus `readiness-assessment-unavailable`, without changing best-effort success or a satisfied C6.3 gate. Complete license policy/global transformation provenance, global build-input closure, reproducibility, signing, actual loader selection/complete transitive closure/`dlopen`/Windows/plugin/executable/crate/Nuitka/WASM/final-authorization work remains absent. |
| Source-transformation inventory observation (C6.6, unreleased) | Experimental | `artifact_evidence.source_transformation_inventory` binds accepted project-owned native functions to source path/hash, module/qualname, reliable range, semantic-AST hash, exact generated Rust input, closed generator/backend id, and sorted plugin ids. Collection cross-checks the analyzer list used by code generation and caps records, plugin references/ids, and total serialized characters. Raw source/AST dumps/absolute paths are excluded. Readiness reconstructs the closed model and exact evidence-reference bindings but does not independently re-derive qualname/range/semantic hash. It is observation-only and `complete: false`; it does not satisfy `source-transformation-provenance-complete`. |
| Scoped source-transformation replay verification (C6.10, unreleased) | Experimental | `artifact_evidence.source_transformation_verification` securely rereads the exact project-source set, rederives qualnames/ranges/semantic AST identities, relowers the full accepted closure, and requires byte-identical full `src/lib.rs` regeneration. Scope `project-functions-pyo3-plugin-free-v1` excludes plugins, embedding, native top-level, runtime shims, delegated fallback, boundary calls, and external source. `complete_for_scope: true` coexists with `global_provenance_complete: false`; the global readiness check and distribution authority remain blocked. |
| Component-license inventory observation (C6.7, unreleased) | Experimental | `artifact_evidence.component_license_inventory` exactly covers every reachable Cargo package, including the path root, in canonical `bom_ref` order. Null/blank values are `missing`; other bounded metadata strings are preserved verbatim as `declared-unvalidated`. No SPDX parsing, normalization, license-file reading, allow/deny policy, legal approval, or authorization is claimed. At the current provenance ceiling C6.7 is omitted after C6.15/C6.14/C6.13/C6.12/C6.11/C6.10/C6.9/C6.8 and before C6.6, and `component-license-policy-complete` remains blocked. |
| Scoped Cargo component-license policy verification (C6.11, unreleased) | Experimental | `artifact_evidence.component_license_policy_verification` binds a strict project-owner lock to the full C6.7 digest, exact raw registry rows, and exact lock bytes. The receipt covers only the fixed local-build/package/redistribution decision and is omitted after C6.15/C6.14/C6.13/C6.12 but before earlier evidence at the current ceiling or on final reread mismatch. Attestor identity, SPDX/license-file/NOTICE/obligation/compatibility checks, legal approval, signatures, global license-policy completion, and distribution authority are not verified. |
| Scoped project-source license-policy verification (C6.12, unreleased) | Experimental | `artifact_evidence.project_source_license_policy_verification` exists only with a valid C6.10 receipt and binds its canonical digest, exact project-source set, generated Rust `src/lib.rs`, separate owner-declared source/output licenses, and exact `rextio.source-license.lock.json` bytes. Final collection reruns C6.10 with identical inputs and then recollects C6.12; source/output/lock change or ceiling pressure omits only C6.12 after C6.15/C6.14/C6.13. It verifies no identity, SPDX, license/NOTICE files, obligations, compatibility, ownership, derivative-work rights, legal approval, signature, global policy, or distribution authority. |
| Scoped analysis-input verification (C6.13, unreleased) | Experimental | Optional `artifact_evidence.analysis_input_verification` receipt for every C6.10 source's sibling `.pyi` state. Present stubs bind logical path, byte SHA-256/size, and deterministic supported-signature projection/version to the exact C6.10 replay/source set and become `project-python-stub` materials; absent records remain metadata only. Secure immutable snapshots are evidence-eligible, while compatibility snapshots are analyzer-only. `complete_for_scope` is limited to this sibling-stub scope; global input closure, reproducibility, signing, policy satisfaction, and distribution authorization remain blocked. |
| Artifact-policy coverage inventory (C6.14, unreleased) | Experimental | Optional `artifact_evidence.artifact_policy_coverage_inventory` partitions only the exact C6.2-C6.13 observed component universe into thirteen disjoint classes with compact count/set digests. Identity strength, scoped owner-receipt license coverage, and replay/input provenance are orthogonal. It is unsigned metadata, omitted after C6.15 at the sidecar ceiling, and never claims global policy/provenance completeness or distribution authority. |
| Scoped artifact-class policy verification (C6.15, unreleased) | Experimental | Optional `artifact_evidence.artifact_class_policy_verification` binds strict `rextio.artifact-policy.lock.json` bytes to the full semantic C6.14 digest, canonical partition digest, and exactly thirteen ordered nested class rows. A deterministic closed disposition matrix preserves all applicable C6.10-C6.13 receipt bindings. The lock is exactly one provenance material outside C6.14 and is omitted first; identity/SPDX/files/notices/obligations/compatibility/ownership/rights/legal/provenance/signature/global-policy/distribution claims remain false. |
| Full-C6/C5.2 frozen Alpha (2.21.0-2.24.0, unreleased) | Experimental | `artifact_distribution_policy = "full-c6-required"` accepts only CPython 3.11/PyO3/Cargo on macOS arm64 or Linux x86_64, a non-editable RECORD-verified Rextio install, a sealed bounded Python namespace with no `.rextioignore`, owner-pinned offline Cargo lock/vendor inputs, exactly one SourceLock-authorized depth-1 `py3-none-any` package, and direct typed scalar leaf calls. The installed host must be cache-free (`pip --no-compile`, bytecode disabled, no `__pycache__`/`.pyc` or unrecorded tree entry) and fit the 256 MiB declared-plus-actual input budget; this protects evidence integrity and is not hostile-process secure boot. Bootstrap v2 publishes exact C6.14+C5.2 rows/transformations and internal/external license observations; the owner separately supplies explicit row allows/transformation acceptance and runs `rextio policy finalize` for manifest v2. Every strict build stage recollects and performs two actual isolated builds. The configured support lock binds a process-sealed plan plus raw/Merkle digests; its output cannot exact/ancestor/descendant-alias any configured lifecycle artifact or source archive. Its complete tree is verified once during host collection and twice inside the executor for each strict build, while intermediate boundaries check the seal, critical leaves, and digest identity without a full walk. Linux executes through bwrap→sealed-seccomp→isolated-CPython→Landlock→Cargo; macOS uses sandbox-exec with Xcode/SDK/SSV anchors and denies inherited executable mappings under mutable/data-volume roots, regranting map/exec only to explicit bound read-execute paths or bound read-write directories. `sandbox_profile_sha256` is the path-tokenized engine-specific semantic identity, equal across both builds and equivalent lifecycles; raw rendered private-path profiles are neither public nor signed. Invocation, SBOM, and SLSA receipts are otherwise path-free. One local macOS run observed about 104,645 members / 2.67 GB and 45 seconds per full verification; this is not a guarantee. The scrubbed executor binds only the active target's exact identity-verified linker variable and rejects overrides/inactive bindings. macOS uses stable `@rpath/lib_rextio_native.dylib` with the default content-derived deterministic `LC_UUID`; its closed dyld shared-cache policy admits exact observed `/usr/lib/system/libcommonCrypto.dylib` only as a one-hop provider for `/usr/lib/libSystem.B.dylib`, with final-snapshot and scoped/global/`dladdr` agreement—not as a broad descendant allowance. The final local real-E2E at `f9eb5e6` certifies the historical macOS arm64 installed-wheel lifecycle; subsequent byte-budget and support-lock/sandbox hardening is unit-tested, while exact-HEAD macOS arm64 and Linux x86_64 heavy E2E remain pending until branch push and blocking CI. Exact `Requires-Dist` stays; the runtime guard walks from `/` component-wise with no-follow directory descriptors and a nonblocking final open, rejecting linked roots/ancestors, FIFOs, and special files without importing/introspecting the external module/callable. Final authorization verifies an exact domain-separated Ed25519 message and canonical detached envelope, then publishes six bounded payloads plus the seventh binding manifest atomically. Exact external PEP 639 payloads become output-wheel license members. That closed set is capped at 128 files/64 MiB (the frozen real PyO3 graph uses 108); 129 files or path/alias violations fail closed. Cargo license ingestion recognizes only exact legacy `MIT/Apache-2.0` as canonical `MIT OR Apache-2.0` while retaining original manifest evidence; other slash variants fail closed. `FullC6ProductionAuthority` is process-local evidence only. Cargo pins do not authenticate registry/publisher origin. Plugins, executable, rust-crate, top-level AOT, embedding, Windows, recursion, and general package lowering are excluded. The Alpha does not defend against hostile same-UID concurrent replacement, kernel/OS compromise, or provide complete time/randomness/scheduling/CPU virtualization; it is not a general hermetic-build claim. |
| One-hop native path-resolution observation (C6.8, unreleased) | Experimental | `artifact_evidence.native_runtime_path_resolution` exactly covers C6.4 direct dependencies. System names are logical leaves; contained Mach-O loader-path/self-rpath and Linux ORIGIN RPATH/RUNPATH candidates bind one unique non-symlink wheel member by hash/size. Missing or unsafe data omits C6.8 and dependent C6.9; malformed present data fails readiness reconstruction closed. This is static packaged-candidate observation, not actual loader selection, complete transitive closure, system-library byte evidence, or `dlopen` coverage. |
| Bounded static native-runtime graph observation (C6.9, unreleased) | Experimental | `artifact_evidence.native_runtime_transitive_closure` recursively inspects only exact C6.8-reached packaged members, preserves cycles as canonical edges, and records system names as unhashed terminal leaves. Exact wheel/snapshot/receipt identity and strict node/edge/depth/candidate/inspector/output/deadline/serialization bounds fail closed. C6.9-only failure retains C6.8; the current provenance ceiling omits C6.15, C6.14, C6.13, C6.12, C6.11, and C6.10 before C6.9. It explicitly does not satisfy `native-runtime-transitive-closure-complete`, choose an actual loader result, consult ambient state, observe `dlopen`, or authorize distribution. |
| Nuitka fallback / executable backend | Experimental | `--fallback=nuitka`, `--executable-backend=nuitka`. Requires Nuitka; surfaced by the build preflight when missing. The real-Nuitka end-to-end path runs only on the scheduled/manual CI job (not on every PR), so regressions there may surface later than for the Cargo path. |
| Build incrementality | N/A (clean rebuild) | `rextio build`/`generate` fully re-analyze, re-lower, and re-render on every invocation and reset `.rextio/build`/`.rextio/generated` first; there is no mtime- or fingerprint-based incremental caching in the 0.1.x line (through released 0.1.4). A no-op rebuild does the full work. Reports (`build.json`/`generate.json`/`check.json`) are reset each run so only the current command's reports remain. |
| Hybrid runtime `__file__` | Experimental (limitation) | The subprocess-hybrid executable backend copies the project's Python source into `<binary>.runtime/`, so a delegated fallback function's `__file__` points at the copied tree, not the original source. Code that resolves data files relative to `__file__` (e.g. `Path(__file__).parent / "data"`) will not find them there; keep such data-dependent functions native, or use the source hybrid runtime against the original tree. |
| Rust-importable crate artifact | Experimental | `--rust-importable` / `--rust-crate-name`. Exposes accepted direct-Rust functions as a Cargo path dependency. |
| Plugins | Experimental | Entry-point plugins declare target compatibility and the external packages they cover. Protocol v2 (see [the tooling contract](specs/tooling-contract.md)) supports self-described rules and `RXTP-<PLUGIN>-NNN` diagnostics. API 1.1+ lowers through claim/lower hooks; 1.2 adds version-gated keyword/literal metadata, fusion trees, and `operand_mode` direct\|leaves; 1.3 (core 0.1.3; published core 0.1.4) adds resident values/native chaining, receiver/callable/schema metadata, and bool/string static keyword literals. Unreleased Train C advertises API **1.4** (package version still 0.1.4) for optional standalone artifact capability. Dynamic keyword values, `**kwargs`, floats, and bytes remain fail-closed ([plugin lowering spec](specs/plugin-lowering.md)). Older providers keep their legacy offer/serialization shapes. The analyzer remains the acceptance authority. |
| Plugin standalone artifact capability (Train C, unreleased) | Experimental | Plugin API **1.4** optional `artifact_capability(profile)` hook: fail-closed, explicit declaration of boundary-free standalone support for exact `rust-crate` / `host-executable` profiles. Not part of the all-or-none lowering member set; absence means standalone unsupported. Core never infers support from host-extension conversion, resident status, uses/helpers, or crate deps. `rextio capabilities` reports presence-only `artifact_capability_declared` (no profile-hook execution); generate/build may emit resolved `standalone_plugin_capabilities` allow/deny details. Tooling contract **2.4.0** additive shape; package version remains **0.1.4**. See [plugin lowering](specs/plugin-lowering.md) §10 and [the tooling contract](specs/tooling-contract.md). |
| Tooling-contract JSON fields | Experimental | `rextio check --format json` (and `.rextio/reports/check.json`) carries a top-level `contract_version` (`2.2.0` in published core 0.1.4) plus per-function route and promotion data, as specified in [the tooling contract](specs/tooling-contract.md). Contract `2.0.0` (core 0.1.2) broke `RXT000` column semantics; `2.1.0` (core 0.1.3) added logger/claim metadata; `2.2.0` adds `marker_kind`, isolated `promotion_assessment`, and reliable function/name ranges while preserving `route`, `native_status`, and `rejection_codes` semantics. The unreleased Train C producer (package version still 0.1.4) advances additively through `2.3.0`-`2.20.0` host/external-source preview contracts, **`2.21.0`** strict Full-C6 primitives, **`2.22.0`** bounded C5.2/initial CLI coordination, historical **`2.23.0`** exact policy handoff and closure hardening, and current **`2.24.0`** support-lock bootstrap plus sandbox/support receipt surfaces. Preview readiness remains non-authorizing; `lowering_provided` / route / native-status / rejection / promotion-assessment semantics remain unchanged. Dual-map `2.x` consumers that ignore unknown keys remain compatible; the shape may still be refined until the contract is promoted to stable. |
| `rextio capabilities` command | Experimental | Prints the config-resolved capability manifest from [the tooling contract](specs/tooling-contract.md): the supported type matrix, core and active-plugin rule records (constraint + diagnostic code + guidance per rule), a `config_fingerprint` for consumer caching, and the active plugins. Introspection-only (no source analysis, no report files). |
| Draft device-provider records (Train C, unreleased) | Experimental | Design-draft `manifest()` / `preflight()` records only. There is no provider discovery, selection, build/link integration, or runtime dispatch. Every preflight result has `support_claim: false`. See the [device-provider draft](specs/device-provider.md). |
| CUDA Driver API inventory probe (Train C, unreleased) | Experimental | Bounded Windows x64 + Linux x86_64/aarch64 validation tool only; it does not create a context, manage memory, launch a kernel, or certify CUDA. Every report has `support_claim: false`. See [CUDA driver validation](testing/cuda-driver-validation.md). |

## Planned (not implemented)

| Feature | Tier | Notes |
| --- | --- | --- |
| Additional native target languages | Planned | `native_backend` accepts only `rust` in 0.1.0; any other value is rejected at configuration load with a clear error. |
| General external pure-Python package source-AOT | Planned | The frozen Full-C6/C5.2 Alpha proves only one SourceLock-authorized depth-1 `py3-none-any` dependency and direct typed scalar leaf calls on two host targets. Multiple or recursive packages, general value flow/calls, plugins, vendoring, broader platforms/artifacts, and general redistribution remain unimplemented. |
| Device-provider build/runtime integration and first-party CUDA | Planned | Train C defines only non-operational draft records and an inventory probe. Provider discovery, privileged link/build hooks, kernel execution, and certification are deferred. |

If a feature you depend on is **Experimental**, pin your Rextio version and watch the
changelog; if it is **Planned**, expect a diagnostic rather than a build.
